# Base de datos
DATABASE_URL=postgresql://chatpro:chatpro@localhost:5432/chatpro

# Auth — generar con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=cambia-esto-en-produccion

# Redis
REDIS_URL=redis://localhost:6379/0

# App
APP_ENV=development
PUBLIC_BASE_URL=http://localhost:8000

# WAHA (Fase 1)
# WAHA_API_URL=http://localhost:3000
# WAHA_API_KEY=
# WAHA_WEBHOOK_TOKEN=

# IA (Fase 2)
# ANTHROPIC_API_KEY=

# Conectores (Fase 5) — KEK 32 bytes hex para cifrado AES-GCM de credenciales
# Generar con: python -c "import secrets; print(secrets.token_hex(32))"
# CONNECTOR_MASTER_KEY=
